# qa-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace:  qa
spec:
  selector:
    app: web-server
  ports:
  - port: 80
    targetPort: 80
  type: LoadBalancer   # External access for QA testers