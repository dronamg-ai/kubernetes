# dev-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: dev
spec:
  selector:
    app: web-server
  ports:
  - port: 80
    targetPort: 80
  type: NodePort       # Easy local access for devs